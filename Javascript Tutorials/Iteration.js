// Creating while loop
while(isTrue) {
    // Repeats this while condition is true
}

// Creating a for loop
for (var i = 0; i < 5, i++) {   // i++ can be changed to increment between whaaatever number you want, so oonly even or odd if you want it
    // Code in here runs 5 times
}
// You can make i also decrease so that it counts backwards

// Iterate through array with for loop
var array = [0,1,2,3,4];
for (var i = 0; i < array.length, i ++) {
    // Does this code to each element in array
}